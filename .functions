g() {
   if [ "$1" = "adds" ]; then
      git-add
   fi
   if [ "$1" = "fu" ]; then
      git-fixup
   fi
   if [ "$1" = "sha" ]; then
      git-sha
   fi
   if [ "$1" = "co" ]; then
      git-checkout
   fi
   if [ "$1" = "rbs" ]; then
      git-rebase
   fi

   command git "$@"
}
